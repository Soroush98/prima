name: CI & Deploy to Fly.io

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

# Never run two deploys of the same ref at once.
concurrency:
  group: deploy-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # ---- Gate: lint, typecheck, and a real production build must pass ----
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run lint
      - run: npx tsc --noEmit
      - run: npm run build

  # ---- Which parts changed? (skip the slow PyTorch deploy when only web changed) ----
  changes:
    needs: verify
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    outputs:
      ml: ${{ steps.filter.outputs.ml }}
    steps:
      - uses: actions/checkout@v5
      - uses: dorny/paths-filter@v4
        id: filter
        with:
          filters: |
            ml:
              - 'ml-service/**'

  # ---- Web app (deployed on every push to main) ----
  deploy-web:
    needs: verify
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    # Job-level env so both setup-flyctl and the deploy step see the token.
    env:
      FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
    steps:
      - name: Check FLY_API_TOKEN is set
        run: |
          if [ -z "$FLY_API_TOKEN" ]; then
            echo "::error::FLY_API_TOKEN secret is missing. Create an ORG-scoped token (it deploys both prima-web and prima-ml) with 'fly tokens create org <org> -x 8760h' and add it with 'gh secret set FLY_API_TOKEN'."
            exit 1
          fi
      - uses: actions/checkout@v5
      - uses: superfly/flyctl-actions/setup-flyctl@master
      # Fail fast with a clear identity error if the token is invalid/expired/mis-scoped,
      # instead of a cryptic "unauthorized" mid-deploy.
      - run: flyctl auth whoami
      # --yes: auto-confirm non-interactive prompts (e.g. detaching the now-unused
      # prima_data volume after the warehouse moved into the image).
      - run: flyctl deploy --remote-only --yes

  # ---- ML ensemble (only when ml-service/** changed) ----
  deploy-ml:
    needs: changes
    if: needs.changes.outputs.ml == 'true'
    runs-on: ubuntu-latest
    # Job-level env so both setup-flyctl and the deploy step see the token.
    env:
      FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
    steps:
      - name: Check FLY_API_TOKEN is set
        run: |
          if [ -z "$FLY_API_TOKEN" ]; then
            echo "::error::FLY_API_TOKEN secret is missing. Create an ORG-scoped token (it deploys both prima-web and prima-ml) with 'fly tokens create org <org> -x 8760h' and add it with 'gh secret set FLY_API_TOKEN'."
            exit 1
          fi
      - uses: actions/checkout@v5
      - uses: superfly/flyctl-actions/setup-flyctl@master
      # Fail fast with a clear identity error if the token is invalid/expired/mis-scoped,
      # instead of a cryptic "unauthorized" mid-deploy.
      - run: flyctl auth whoami
      - run: flyctl deploy --remote-only --yes
        working-directory: ml-service
