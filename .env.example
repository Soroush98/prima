# Copy this file to `.env` and add your key. REQUIRED — Prima's agents call a
# live Claude model on every request (there is no offline path).
ANTHROPIC_API_KEY=

# Which Claude model the agent fleet uses.
PRIMA_MODEL=claude-sonnet-4-6
